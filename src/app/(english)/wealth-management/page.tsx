import { WealthManagement } from "@/components/inner-pages";
import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("en","wealth-management");
export default function Page(){return <WealthManagement locale="en"/>;}
