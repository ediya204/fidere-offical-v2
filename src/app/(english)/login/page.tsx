import { Login } from "@/components/inner-pages";
import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("en","login");
export default function Page(){return <Login locale="en"/>;}
