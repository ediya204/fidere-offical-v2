import { Compliance } from "@/components/inner-pages";
import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("en","compliance");
export default function Page(){return <Compliance locale="en"/>;}
